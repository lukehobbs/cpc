BoolFlags:
  - Name:  "full, f"
    Usage: "Used if a full end-to-end pipeline is desired"
StringFlags:
  - Name:  "name, n"
    Usage: "Name to be associated with the stack"
IntFlags:
  - Name:  "persist, p"
    Value: -10
    Usage: "How long should this stack exist?"
